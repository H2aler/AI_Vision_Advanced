{
  "name": "geometry",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix server run dev",
    "build": "npm --prefix server run build",
    "start": "npm --prefix server start"
  }
}
